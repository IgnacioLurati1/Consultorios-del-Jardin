# coverage reports
coverage

# API keys and secrets
.env

.prettierrc

# dependency directory
node_modules

# OS metadata
.DS_Store
Thumbs.db

# ignore built ts files
**/dist/**

# Caché de la compilación incremental de TypeScript. Es estado local de una máquina:
# versionarlo hace que otra compile a partir de lo que se compiló acá, y con dist/
# ignorado eso significa arrancar creyendo que ya existe lo que no existe.
*.tsbuildinfo

# vscode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json 
!.vscode/extensions.json
!.vscode/*.code-snippets

## local jistory for vscode
.history/

## built vscode extensions
*.vsix

## TEMPORAL API PROTECTION
/backend/src/config/serviceAccountKey.json

## Claude generated files

CLAUDE.md