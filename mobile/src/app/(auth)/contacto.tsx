import { ContactForm } from "../../features/ContactForm";

/** Escribirle al consultorio sin tener cuenta. El encabezado lo pone el navegador. */
export default function ContactFromLogin() {
  return <ContactForm />;
}
